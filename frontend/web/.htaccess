Options -Indexes

RewriteEngine on

# Serve existing files/directories directly.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Route everything else to Yii front controller.
RewriteRule . index.php [L]
